---
# Whether to install docker compose.
docker_compose: true

# A list of users that should be added to the docker group.
docker_users: []

# Whether to install packages required for the ansible docker modules.
ansible_modules: false
